a = ["ant", "bat", "cat", 42];
a.forEach(element => {
  console.log(element);
})

soliloquy = "To be, or not to be, that is the question:";
Array.from(soliloquy).forEach(function(element) {
  console.log(element);
})